// Função para pesquisa (apenas um exemplo simples)  

function searchFunction() { const query = document.getElementById('searchInput').value; if(query) { alert('Você está buscando por: ' + query); } else { alert('Digite algo na barra de pesquisa!'); } } 
